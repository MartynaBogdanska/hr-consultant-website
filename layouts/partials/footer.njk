 <footer class="bg-gradient py-3">
    <div class="container">
        <p class="small text-white my-0">© 2020 Resume Matters 
            {% for link in global.site.footerlinks %}
            | {% if link.external %}<a href="{{ link.href }}" target="_blank" rel="noopener noreferrer">{{ link.label }}</a>
                {% else %} <a href="{{ relative }}{{ link.href }}">{{ link.label }}</a>
            {% endif %}
            {% endfor %}
        </p>
    </div>
</footer>